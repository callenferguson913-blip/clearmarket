[phases.install]
cmds = ["pip install --no-cache-dir fastapi 'uvicorn[standard]' jinja2 python-multipart sqlalchemy psycopg2-binary python-dotenv yfinance requests anthropic==0.50.0"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
